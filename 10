import java.util.HashMap;

public class Counthow10 {
    public static void main(String[] args) {
        HashMap<String, Integer> scores = new HashMap<>();
        scores.put("Aida", 85);
        scores.put("Azat", 92);
        scores.put("Dana", 77);

        int count = 0;
        for (int score : scores.values()) {
            if (score >= 80) {
                count++;
            }
        }
        System.out.println("Students with score >= 80: " + count);
    }
}
